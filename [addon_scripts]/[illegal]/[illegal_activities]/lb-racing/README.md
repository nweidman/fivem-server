# LB Racing

## Documentation

https://docs.lbscripts.com/racing/

## Installation

1. Add `lb-racing` to your `resources` folder
2. Add `start lb-racing` to your `server.cfg`
3. Restart your server, or start the resource by running the commands `refresh` and `start lb-racing`
