------------------------------------------------------------------------------
-- OPEN STASH FUNCTION / this is called when user press 'e' or use target ----
------------------------------------------------------------------------------

if Config.Inventory.inventoryScript ~= "none" then return end

function openStash(orgId)
end

function openFurnitureStorage(stashName, capacity, slots, label)
    print('Furnitures Storage is not supported while not using any inventory script!!!!!')
end